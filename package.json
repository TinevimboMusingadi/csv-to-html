{
  "name": "csv-to-html",
  "version": "1.0.0",
  "description": "Turn CSV into an HTML table (runs with local server)",
  "scripts": {
    "start": "serve .",
    "dev": "serve . -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
